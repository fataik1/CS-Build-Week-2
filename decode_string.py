class Solution:
    def decodeString(self, s: str) -> str:
        #will use stack
        #iterate through the string
        # push everything to stack
        # find the position and then pop from stack
        
        